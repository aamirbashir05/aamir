# AlTariq Photoroom Pro — runtime dependencies
# Pinned for reproducible Windows builds (CPU only, fully offline at runtime).
pillow>=10.2
numpy<2.0
onnxruntime==1.17.3
rembg==2.0.57
pymatting>=1.1.12
scipy>=1.10
pywebview>=5.1
pythonnet>=3.0.3
